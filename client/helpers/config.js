// Accounts.ui.config({
//     passwordSignupFields: "USERNAME_ONLY"
// });

Accounts.ui.config({   // This code allows us to login users via just username instead of email address
    passwordSignupFields: "USERNAME_ONLY"
})